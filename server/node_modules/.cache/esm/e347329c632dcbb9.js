let express,cors,readdirSync,mongoose;_632‍.w("express",[["default",["express"],function(v){express=v}]]);_632‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_632‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_632‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);//entry point of our server
//console.log("server setup")





const morgan = require("morgan"); 
require("dotenv").config();


//creating express app
const app = express();

//db
mongoose.connect(process.env.DATABASE, {
  userNewUrlParser:true,
  useFindAndModify:false,
  useUnifiedTopology:true,
  useCreateIndex:true,

})
.then(() => console.log("**DB CONNECTED**"))
.catch((err) => _632‍.g.console.log("DB CONNECTION ERR =>", err));

// apply middlewares
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));


//route
readdirSync("./routes").map((r) =>
  app.use("/api", require(`./routes/${r}`))
);
//port
const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on port ${port}`));